version: "3"
services:
  database:
    image: "postgres"
    env_file:
      - db.env
    volumes:
      - ./db.sql:/docker-entrypoint-initdb.d/db.sql # Docker runs this to build the database schema on startup
